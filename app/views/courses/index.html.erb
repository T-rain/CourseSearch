<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Course</title>
</head>
<body>
	<header>
	  <div class="left-nav">
	    <ul>
	      <li><%= link_to "Home", courses_path %></li>
	    </ul>
	  </div>
	  <div class="right-nav">
	    <ul>
	      <li><%= link_to "Add Course", new_course_path %></li>
	    </ul>
	  </div>
	  <div class="clearfix"></div>
	</header>	


	<h1>課程查詢系統</h1>
	
	<%= form_tag courses_path,method:"get" do %>
	<div>
		課程名稱:<%= text_field_tag :course  %>
		老師名稱:<%= text_field_tag :teacher %>
		時間:<%= text_field_tag :time %>
	</div>
	
	<%= submit_tag "search" %>
	<%end%>


	<p>Course List:</p>
	<p> -------------------------------------------------------------------------------------------------------------------</p>
	
	<% @course.each do |course| %>

	  <div>
	  	課程名稱:<%= link_to course.course,course_path(course)%>
	  	<p>課程簡介:時間:<%= course.time%>,老師:<%= course.teacher%></p>
	  	<%= button_to 'Edit', edit_course_path(course),method:"get"%>
  		<%= button_to 'Delete', course_path(course), method:"delete", data: { confirm: 'Delete?' } %>
  		<p> -------------------------------------------------------------------------------------------------------------------</p>
	  </div>


	<% end %>
</body>
</html>